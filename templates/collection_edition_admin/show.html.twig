{% extends 'base.html.twig' %}

{% block title %}CollectionEdition{% endblock %}

{% block body %}
    <h1>CollectionEdition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ collection_edition.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ collection_edition.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ collection_edition.slug }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ collection_edition.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('collection_edition_admin_index') }}">back to list</a>

    <a href="{{ path('collection_edition_admin_edit', {'id': collection_edition.id}) }}">edit</a>

    {{ include('collection_edition_admin/_delete_form.html.twig') }}
{% endblock %}
