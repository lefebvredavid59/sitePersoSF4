{% extends 'base.html.twig' %}

{% block title %}CollectionFamily{% endblock %}

{% block body %}
    <h1>CollectionFamily</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ collection_family.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ collection_family.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ collection_family.slug }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ collection_family.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('collection_family_admin_index') }}">back to list</a>

    <a href="{{ path('collection_family_admin_edit', {'id': collection_family.id}) }}">edit</a>

    {{ include('collection_family_admin/_delete_form.html.twig') }}
{% endblock %}
