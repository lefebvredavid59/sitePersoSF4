{% extends '../../../base.html.twig' %}

{% block title %}CollectionCategory{% endblock %}

{% block body %}
    <h1>CollectionCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ collection_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ collection_category.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ collection_category.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('collection_category_admin_index') }}">back to list</a>

    <a href="{{ path('collection_category_admin_edit', {'id': collection_category.id}) }}">edit</a>

    {{ include('collection_category_admin/_delete_form.html.twig') }}
{% endblock %}
