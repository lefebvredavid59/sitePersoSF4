{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Edition d'Edition de Collection{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="navhead display-2 text-center">Edition d'Edition de Collection</h1>

        <div class="card">
            <div class="card-body shadow-lg">

                {{ include('admin/collection/collection_edition_admin/_form.html.twig',
                    {'button_label': 'Mis a jour'}) }}

                <div class="d-grid">
                    <a href="{{ path('collection_edition_admin_index') }}" class="btn btn-outline-primary">Retour à la
                        liste</a>
                </div>

                {{ include('admin/collection/collection_edition_admin/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}