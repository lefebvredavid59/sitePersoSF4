{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Nouvelle Edition Collection{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="navhead display-2 text-center">Nouvelle Edition Collection</h1>

        <div class="col-6 mx-auto">
            <div class="card">
                <div class="card-body shadow-lg">

                    {{ include('admin/collection/collection_edition_admin/_form.html.twig') }}

                    <div class="d-grid">
                        <a href="{{ path('collection_edition_admin_index') }}" class="btn btn-outline-primary">Retour a
                            la
                            liste</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}