{% extends '../../../base.html.twig' %}

{% block title %}CollectionSubcategory{% endblock %}

{% block body %}
    <h1>CollectionSubcategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ collection_subcategory.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ collection_subcategory.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ collection_subcategory.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('collection_subcategory_admin_index') }}">back to list</a>

    <a href="{{ path('collection_subcategory_admin_edit', {'id': collection_subcategory.id}) }}">edit</a>

    {{ include('collection_subcategory_admin/_delete_form.html.twig') }}
{% endblock %}
