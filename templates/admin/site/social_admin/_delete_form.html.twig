<form method="post" action="{{ path('social_admin_delete', {'id': social.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ social.id) }}">
    <button class="btn">Delete</button>
</form>
