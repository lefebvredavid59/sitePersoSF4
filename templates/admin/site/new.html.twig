{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Nouveau gestion Site{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="navhead display-2 text-center">Nouveau gestion Site</h1>

        <div class="col-6 mx-auto">
            <div class="card">
                <div class="card-body shadow-lg">

                    {{ include('Admin/Site/_form.html.twig') }}

                    <div class="d-grid">
                        <a href="{{ path('site_index') }}" class="btn btn-outline-primary">Retour a la liste</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
