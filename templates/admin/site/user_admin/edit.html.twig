{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Edition {{ user.pseudo }}{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="navhead display-2 text-center">Edition {{ user.pseudo }}</h1>

        <div class="col-6 card mx-auto">
            <div class="card-body shadow-lg">

                {{ include('admin/site/user_admin/_form.html.twig', {'button_label': 'Mis a jour'}) }}
                <div class="d-grid">
                    <a href="{{ path('user_admin_index') }}" class="btn btn-outline-primary">Retour à la liste</a>
                </div>

                {{ include('admin/site/user_admin/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
