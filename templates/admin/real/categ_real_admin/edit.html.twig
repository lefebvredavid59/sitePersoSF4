{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Edition Catégorie Réalisation{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="navhead display-2 text-center">Edition Catégorie Réalisation</h1>

        <div class="card">
            <div class="card-body shadow-lg">

                {{ include('Admin/real/categ_real_admin/_form.html.twig', {'button_label': 'Update'}) }}
                <div class="d-grid">
                    <a href="{{ path('categ_real_admin_index') }}" class="btn btn-outline-primary">Retour à la liste</a>
                </div>

                {{ include('Admin/real/categ_real_admin/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}