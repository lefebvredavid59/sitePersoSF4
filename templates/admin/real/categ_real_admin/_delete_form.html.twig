<form class="d-grid" method="post" action="{{ path('categ_real_admin_delete', {'id': categ_real.id}) }}"
      onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet catégorie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categ_real.id) }}">
    <button class="btn btn-outline-danger my-3">Supprimer</button>
</form>
