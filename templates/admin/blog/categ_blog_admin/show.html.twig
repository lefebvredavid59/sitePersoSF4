{% extends 'base.html.twig' %}

{% block title %}CategBlog{% endblock %}

{% block body %}
    <h1>CategBlog</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categ_blog.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categ_blog.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ categ_blog.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categ_blog_admin_index') }}">back to list</a>

    <a href="{{ path('categ_blog_admin_edit', {'id': categ_blog.id}) }}">edit</a>

    {{ include('categ_blog_admin/_delete_form.html.twig') }}
{% endblock %}
