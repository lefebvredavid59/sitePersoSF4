<form method="post" action="{{ path('categ_blog_admin_delete', {'id': categ_blog.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categ_blog.id) }}">
    <button class="btn">Delete</button>
</form>
