{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Edition Catégorie blog{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="navhead display-2 text-center">Edition Catégorie Blog</h1>

        <div class="card">
            <div class="card-body shadow-lg">

                {{ include('admin/blog/categ_blog_admin/_form.html.twig', {'button_label': 'Mis a jour'}) }}

                <div class="d-grid">
                    <a href="{{ path('categ_blog_admin_index') }}" class="btn btn-outline-primary">Retour à la liste</a>
                </div>

                {{ include('admin/blog/categ_blog_admin/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}