{% extends 'base.html.twig' %}

{% block title %}CategReal{% endblock %}

{% block body %}
    <h1>CategReal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categ_real.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categ_real.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ categ_real.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categ_real_admin_index') }}">back to list</a>

    <a href="{{ path('categ_real_admin_edit', {'id': categ_real.id}) }}">edit</a>

    {{ include('categ_real_admin/_delete_form.html.twig') }}
{% endblock %}
