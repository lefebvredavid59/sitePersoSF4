<form method="post" action="{{ path('categ_real_admin_delete', {'id': categ_real.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categ_real.id) }}">
    <button class="btn">Delete</button>
</form>
