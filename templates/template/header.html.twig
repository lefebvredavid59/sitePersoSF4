<nav class="navbar navbar-expand-lg navbar-light bg-transparent">
    <div class="container-fluid">
        <a href="{{ path('home') }}" class="navbar-brand navhead">
            <i class="fa-solid fa-d fa-3x"></i>
            <i class="fa-solid fa-l fa-2x"></i>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a href="{{ path('home') }}" class="nav-link navhead" aria-current="page">
                        Accueil
                    </a>
                </li>

                {% if blogs is not empty %}
                <li class="nav-item">
                    <a href="{{ path('blog') }}" class="nav-link navhead">
                        Blog
                    </a>
                </li>
                {% endif %}

                {% if reals is not empty %}
                <li class="nav-item">
                    <a href="{{ path('realization') }}" class="nav-link navhead">
                        Realisation
                    </a>
                </li>
                {% endif %}

                <li class="nav-item">
                    <a href="{{ path('about') }}" class="nav-link navhead">
                        Moi
                    </a>
                </li>

                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a href="{{ path('admin') }}" class="nav-link navhead">
                            Admin
                        </a>
                    </li>
                {% endif %}

                {% if app.user %}
                    <li class="nav-item">
                        <a href="{{ path('app_logout') }}" class="nav-link navdeco">
                            déconnexion
                        </a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a href="{{ path('app_login') }}" class="nav-link navlog">
                            connexion
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>